# aOS Kernel Modules

This directory contains kernel modules (.akm) for aOS to be loaded via AKM VM or APM

Kernel modules extend the functionality of the aOS kernel with drivers, filesystems, and other kernel-level features.

aOS Package Manager (APM) by default uses this folder for kmodules unless otherwise configured.
