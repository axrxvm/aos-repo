# Kernel Modules

This directory contains kernel modules for the i386 architecture.

Kernel modules extend the functionality of the aOS kernel with drivers, filesystems, and other kernel-level features for i386 systems.
