# Kernel Modules

This directory contains kernel modules for the x86_64 architecture.

Kernel modules extend the functionality of the aOS kernel with drivers, filesystems, and other kernel-level features for x86_64 systems.
